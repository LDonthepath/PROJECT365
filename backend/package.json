{
  "name": "project365-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/**/*.test.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
